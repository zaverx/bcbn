language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bcbn' ]; then cd .. && eval "mv $currentfolder generator-bcbn" && cd generator-bcbn; fi

